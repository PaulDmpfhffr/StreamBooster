export enum ProxyType {
  RESIDENTIAL = 'residential',
  MOBILE = 'mobile',
  DATACENTER = 'datacenter',
}

export enum ProxyAdapterType {
  IPROYAL = 'iproyal',
  BRIGHTDATA = 'brightdata',
  WEBSHARE = 'webshare',
  MANUAL = 'manual',
}
